# Core — required for server to start
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12
httpx==0.27.2
pydantic==2.9.2
websockets==13.1
qdrant-client==1.12.0
pypdf==5.1.0
python-dotenv==1.0.1
aiofiles==24.1.0
Pillow==10.4.0

# Optional ML — uncomment for real embeddings (falls back to mock without these)
# sentence-transformers==3.3.1
# torch==2.4.0
